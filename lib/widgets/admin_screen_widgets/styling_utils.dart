import 'package:flutter/material.dart';

var boxShadows = const [
  BoxShadow(blurRadius: 4, offset: Offset(3, 4), color: Colors.black38)
];

var stroke =
    Border.all(color: const Color.fromRGBO(205, 205, 205, 100), width: 1);
